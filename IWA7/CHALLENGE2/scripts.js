const nickname= "Timmy";
const firstname = "Timothy";

console.log(`Good Morning, ${nickname || firstname} !` );

//Used string interpolation back ticks
// || logical or- this returns either value
//$not a reserved character and calls the jQuery function which selects everything in brackets