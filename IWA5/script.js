const FREE_WARNING = 'Free shipping only applies to single customer orders';
const BANNED_WARNING = 'Unfortunately we do not ship to your country of residence';
const NONE_SELECTED = '0';


if (location = RSA) { shipping === 400 ; currency ==='R' };

if (location = NAM){
shipping = 600 ; currency === '$'
};
if (location = others){  shipping = 800 ; currency === '$'};
console.log(BANNED_WARNING);
const shoes = 300 * 1 ;
const toys = 100 * 5 ;
const shirts = 150 * 'NONE_SELECTED' ;
const batteries = 35 * 2 ;
const pens = 5 * 'NONE_SELECTED' ;
const totalPrice=(shoes + toys + shirts + batteries + pens)
if (totalPrice>1000)

/*shipping = null
currency = $
console.log(NONE_SELECTED); */

if(shoes + batteries + pens + shirts > 1000 ) {
	if (location = NAM && customers < 2) {
			if (location = RSA)
		    shipping = 0 || calcShipping
		}
	}
console.log(FREE_WARNING)

if (shipping = 0 && customers !== 1) { console.log(WARNING) };
 
if (location = 'NK') console.log(WARNING) ; console.log('price', currency, shoes + batteries + pens + shirts + shipping)

customers = '1'
const location = 'RSA'
currency = null